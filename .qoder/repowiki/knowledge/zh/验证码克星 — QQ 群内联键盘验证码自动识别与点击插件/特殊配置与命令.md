安装方式一：通过 AstrBot WebUI 插件市场搜索 `astrbot_plugin_captcha` 一键安装；方式二：将仓库克隆至 `AstrBot/data/plugins` 后执行 `pip install -r requirements.txt`，并在 WebUI 重载插件生效。需在 WebUI 中填写 `vision_api_key` 与 `vision_model`（如豆包 Endpoint ID）。